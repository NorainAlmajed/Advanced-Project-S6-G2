@model IEnumerable<AdvancedProject.Models.Lease>

@{
    ViewData["Title"] = "Leases";

    var currentSearchTerm = ViewData["CurrentSearchTerm"]?.ToString();
    var currentStatusFilter = ViewData["CurrentStatusFilter"]?.ToString();
    var currentDateFilter = ViewData["CurrentDateFilter"]?.ToString();
    var totalLeases = ViewData["TotalLeases"] ?? 0;
}

<div class="leases-page">
    <div class="d-flex flex-column flex-lg-row align-items-lg-center justify-content-between gap-3 mb-4">
        <div>
            <h1 class="details-title mb-2">Leases</h1>
            <p class="users-subtitle mb-0">
                Browse and manage leases by tenant, unit, property, and status.
            </p>
        </div>
    </div>

    <div class="card search-card users-search-card shadow-soft border-0 mb-4">
        <div class="card-body p-4">
            <form asp-action="Index" method="get">
                <div class="row g-3 align-items-end">
                    <div class="col-lg-5">
                        <label class="form-label fw-semibold text-white">Search Leases</label>
                        <input type="text"
                               name="searchTerm"
                               value="@currentSearchTerm"
                               class="form-control users-search-input"
                               placeholder="Search by lease ID, tenant, unit, or property..." />
                    </div>

                    <div class="col-lg-2">
                        <label class="form-label fw-semibold text-white">Status</label>
                        <select name="statusFilter" class="form-select users-search-input">
                            <option value="">All Statuses</option>
                            <option value="Active" selected="@(currentStatusFilter == "Active")">Active</option>
                            <option value="Terminated" selected="@(currentStatusFilter == "Terminated")">Terminated</option>
                        </select>
                    </div>

                    <div class="col-lg-2">
                        <label class="form-label fw-semibold text-white">Start Date</label>
                        <select name="dateFilter" class="form-select users-search-input">
                            <option value="">All Dates</option>
                            <option value="Latest" selected="@(currentDateFilter == "Latest")">Latest</option>
                            <option value="Oldest" selected="@(currentDateFilter == "Oldest")">Oldest</option>
                        </select>
                    </div>

                    <div class="col-lg-1">
                        <div class="lease-total-box">
                            <span class="lease-total-label">Total</span>
                            <span class="lease-total-value">@totalLeases</span>
                        </div>
                    </div>

                    <div class="col-lg-1">
                        <button type="submit" class="btn btn-primary users-search-btn w-100">
                            Search
                        </button>
                    </div>

                    <div class="col-lg-1">
                        <a asp-action="Index" class="btn btn-outline-light users-reset-btn w-100">
                            Reset
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <div class="users-table-card">
        <div class="table-responsive">
            <table class="table users-table align-middle mb-0">
                <thead>
                    <tr>
                        <th>Lease ID</th>
                        <th>Tenant</th>
                        <th>Unit</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th>Status</th>
                        <th class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-bold">@item.LeaseId</td>

                                <td>@item.Tenant.User.FullName</td>

                                <td>
                                    <div class="fw-semibold">@item.Unit.UnitNumber</div>
                                    <div class="lease-property-name">@item.Unit.Property.Name</div>
                                </td>

                                <td>@item.StartDate.ToString("dd MMM yyyy")</td>
                                <td>@item.EndDate.ToString("dd MMM yyyy")</td>

                                <td>
                                    @if (item.Status == "Active")
                                    {
                                        <span class="custom-pill lease-status-active">Active</span>
                                    }
                                    else if (item.Status == "Terminated")
                                    {
                                        <span class="custom-pill lease-status-terminated">Terminated</span>
                                    }
                                    else
                                    {
                                        <span class="custom-pill badge-soft-default">@item.Status</span>
                                    }
                                </td>

                                <td class="text-center">
                                    <a asp-action="Details"
                                       asp-route-id="@item.LeaseId"
                                       class="btn btn-primary users-details-btn">
                                        View Details
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-center py-4">
                                No leases found.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>